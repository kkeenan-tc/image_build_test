name: Docker build and Bump Version

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  version_bump_docker:
    uses: tucowsinc/wavelo-platforms-automation/blob/WAP-1245/.github/workflows/version_docker.yml@WAP-1245